name: Test PowerShell Script

on:
   push:
     branches: [ main ] 

jobs:
  run-powershell:
    runs-on: windows-latest
    steps:
    - name: checkout code
      uses: actions/checkout@v4

    -name: Run Powershell script
     run: | 
        Write-Host "Running PowerShell script ..."
         pwsh -File Scripts/Envbootstrap.ps1
