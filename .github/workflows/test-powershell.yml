name: Test PowerShell Script

on:
  push:
    branches:
      - main

jobs:
  run-powershell:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run PowerShell script
        run: pwsh -File Scripts/EnvBootstrap.ps1
